{
  "bugFiles": [
    "/home/lewibs/github/shattered-pixel-dungeon-multiplayer/docs/bugs/2026-05-31-lan-attack-action-freeze.md"
  ],
  "notes": [
    "debugger-agent: root cause was actAttack() returning false without clearing curAction, combined with the line-1101 LAN next() guard firing unconditionally on !actResult. Fixed in Hero.java (two surgical changes): (1) added `&& curAction == null` to line-1101 guard, (2) added `curAction = null` before `return false` after sprite.attack() in actAttack(). Test: LanAttackActionFreezeTest."
  ]
}
